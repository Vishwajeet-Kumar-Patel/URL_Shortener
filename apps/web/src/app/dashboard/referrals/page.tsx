"use client";import { useEffect, useState } from "react";import { apiRequest } from "@/lib/api-client";import { useAuthStore } from "@/store/auth.store";type ReferralSummary = { code: string; referralLink: string; totalReferred: number; totalEarnings: number };type ReferralEarning = { id: string; referredUserId: string; amount: number; grossAmount: number; ratePercent: number; createdAt: string };type ReferralEarningsResp = { items: ReferralEarning[] };export default function DashboardReferralsPage() {  const token = useAuthStore((s) => s.accessToken);  const [summary, setSummary] = useState<ReferralSummary | null>(null);  const [items, setItems] = useState<ReferralEarning[]>([]);  useEffect(() => {    const run = async () => {      if (!token) return;      const [s, e] = await Promise.all([        apiRequest<ReferralSummary>("/referrals/me", { token }),        apiRequest<ReferralEarningsResp>("/referrals/earnings?page=1&limit=20", { token })      ]);      setSummary(s);      setItems(e.items);    };    void run();  }, [token]);  return (    <section className="space-y-5">      <h1 className="text-2xl font-semibold text-white">Referrals</h1>      {!summary ? <p className="text-slate-400">Loading...</p> : (        <>          <div className="rounded-xl border border-slate-800 bg-slate-900/70 p-4">            <p className="text-sm text-slate-400">Your referral code</p>            <p className="mt-1 text-xl font-semibold text-white">{summary.code}</p>            <p className="mt-2 break-all text-sm text-indigo-300">{summary.referralLink}</p>            <p className="mt-3 text-sm text-slate-300">Referred: {summary.totalReferred} — Earnings: {summary.totalEarnings}</p>          </div>          <div className="overflow-x-auto rounded-xl border border-slate-800 bg-slate-900">            <table className="min-w-full text-sm">              <thead className="bg-slate-800/70"><tr><th className="px-3 py-2 text-left">User</th><th className="px-3 py-2 text-left">Gross</th><th className="px-3 py-2 text-left">Rate</th><th className="px-3 py-2 text-left">Earning</th></tr></thead>              <tbody>                {items.map((r) => (                  <tr className="border-t border-slate-800" key={r.id}><td className="px-3 py-2 text-slate-300">{r.referredUserId}</td><td className="px-3 py-2">{r.grossAmount}</td><td className="px-3 py-2">{r.ratePercent}%</td><td className="px-3 py-2 text-emerald-300">{r.amount}</td></tr>                ))}              </tbody>            </table>          </div>        </>      )}    </section>  );}